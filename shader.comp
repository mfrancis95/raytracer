#version 440

layout (local_size_x = 1, local_size_y = 1) in;

layout(binding = 0, std430) buffer PixelStorage {
    vec3 pixels[];
};

void main() {
    pixels[int(gl_GlobalInvocationID.x) + int(gl_GlobalInvocationID.y) * 1280] = vec3(1, 0, 0);
}